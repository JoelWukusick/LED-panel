#define ROW_LENGTH 12(number of LEDs in a row)
#define COLUMN_LENGTH 12(number of LEDs in a column)
#define LED_PANEL_PIN D1(control pin for LEDs)

String ssid = "your wifi name";  
String password = "your password";
